/**
 * Identity Module - Address identity and signature tracking
 */

export {
  Identity,
  SignatureEvent,
  IdentityConfig,
  DEFAULT_IDENTITY_CONFIG,
  IdentityQuery,
  IdentityStats,
} from './types';

export { IdentityService } from './service';
